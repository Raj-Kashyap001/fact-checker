<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Get Fact Check - Fact Checking API</title>
    <script src="https://cdn.tailwindcss.com?v=4.1.8"></script>
  </head>
  <body class="min-h-screen flex items-center justify-center bg-gray-50 p-4">
    <div class="w-full max-w-md space-y-4">
      <h1 class="text-xl font-semibold text-gray-800">
        Output: <% if (message) { %>
        <div
          class="p-3 rounded text-black <%= success ? 'bg-green-100' : 'bg-red-100' %>"
        >
          <%= message %>
        </div>
        <% } %>
      </h1>

      <form
        action="/data-text"
        method="POST"
        class="bg-white p-6 rounded-lg shadow space-y-4"
      >
        <textarea
          name="message"
          rows="5"
          class="w-full p-3 border rounded focus:ring focus:ring-blue-500 resize-y"
          placeholder="Enter the text to process"
        ></textarea>

        <button
          type="submit"
          class="w-full bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 rounded transition"
        >
          Submit
        </button>
      </form>
    </div>
  </body>
</html>
